CREATE TABLE if not exists dive (
    id varchar(50),
    diver_address varchar(50),
    place_id varchar(50),
    dive_master_address varchar(50),
    nft_address varchar(50),
    PRIMARY KEY (id)
);